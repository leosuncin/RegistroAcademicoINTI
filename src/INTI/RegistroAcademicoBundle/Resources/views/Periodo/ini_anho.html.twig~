{% extends 'RegistroAcademicoBundle:Default:layout.html.twig' %}

{% block breadcrumb %}
<li><span class="divider">&gt;</span><a href="{{ path('index') }}">Inicio</a></li>
<li><span class="divider">&gt;</span><a href="#">Periodos</a></li>
<li class="active"><span class="divider">&gt;</span>Abrir</li>
{% endblock %}

{% block content %}

    <h1>Abrir Año Escolar </h1>
{{ form_start(form) }}

	{{ form_label(form.anhoCorriente) }}
	{{ form_errors(form.anhoCorriente) }}
    {{ form_widget(form.anhoCorriente) }}

    <div class="form-actions">
       <input  type="submit" value="Crear" class="btn btn-primary" />
    </div>

	{{ form_end(form)}}

        <ul class="record_actions">
    <li>
        <a href="{{ path('periodo') }}">
			Ver Periodos
        </a>
    </li>
</ul>
{% for flashMessage in app.session.flashbag.get('notice') %}
<div class="alert alert-warning">
    <button type="button" class="close" data-dismiss="alert">&times;</button>
    {{ flashMessage }}
</div>

{% endfor %}
{% endblock %}
